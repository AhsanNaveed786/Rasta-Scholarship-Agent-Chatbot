# rasta-agent

A plain Python project for Rasta Agent.

## Setup

1. Install requirements:
   ```
   pip install -r requirements.txt
   ```
2. Copy `.env.example` to `.env` and add your OpenAI API key.
3. Run:
   ```
   python src/main.py
   ```
